package jmxmapper

import org.scalatest.FreeSpec

class Test extends FreeSpec {

//  case class Person(name: String, age: Int)

  "test 1" in {

//    case class Item(name: String, price: Double)
//
//    val mappable = Mappable.materializeMappable[Item]
//
//    val map = mappable.toMap(Item("a", 2.2d))
//    assert(map.get("name") === Some("a"))
//    assert(map.get("price") === Some(2.2d))
//
//    val item = mappable.fromMap(Map("name" -> "dinner", "price" -> 25.8d))
//    assert(item.name === "dinner")
//    assert(item.price === 25.8)

  }

}


